import { Outlet } from "react-router-dom";

const ApprovalManagementLayout = () => {
  return (
    <div className="page-container">
      <Outlet />
    </div>
  );
};

export default ApprovalManagementLayout;
